on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
     - name: checkout code
       uses: actions/checkout@v2
     - name: list the name of the repository
       run: ${{ GITHUB_WORKSPACE }}
