name: CI build

on:
 push:
  branches: ["main"]
 workflow_dispatch:

permissions:
 contents: read

jobs:
 build:
  runs-on: self-hosted

  steps:
   - name: checkout
     uses: actions/checkout@v3
   - name: Build Docker image
     run: docker build -t my-docker-image . 
